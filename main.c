
//#include "library.h"
#include <stdio.h>

int main(void) {
    //printf("\nEnter the input as 'sin 90' and 'asin 0.5' for inverse and 'exit' to exit:\n");
    //char a[N];
    // scanf("%s",&a);
    // getc(stdin);
    //while(getc(stdin));
    //super_loop(*a);
   // printf("%s",calc());
   //calc();
}

